# v36.108 Deployment Checklist

Upload all root files together. Confirm `version.json` reports 36.108 and the page reloads after the service worker update. Do not clear site data; this build is designed to preserve and normalize existing state.
