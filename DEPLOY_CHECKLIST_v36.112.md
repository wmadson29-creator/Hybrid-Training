# Deploy v36.112

Upload every file in this folder together to the repository root. Do not clear app data. After update, open a Half-Kneeling Landmine Press or Landmine Row log/prescription and verify the load helper says `on loaded end` and shows no plates on the anchored end.
