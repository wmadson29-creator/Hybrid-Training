# Deploy v36.110

Upload all files in this folder together to the repository root. Do not clear app data. After the installed app updates to 36.110, open Today once so the failing primary recommendation path is exercised, then tap More > Export Data.
