# Hybrid Training v36.110 QA

Purpose: diagnostic hotfix. The release criterion is that Export Data still produces a JSON file when `utmModelAudit()` or `bodyweightTrend()` throws, and that the export contains raw state plus stack-bearing runtime diagnostics. Recommendation ranking is intentionally unchanged from v36.109.
