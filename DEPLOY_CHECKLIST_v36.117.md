# Deploy v36.117

1. Upload all deployment files together to the repository root.
2. Do not clear app data or re-import it.
3. Confirm `version.json` reports `36.117` and references the v36.117 shell assets.
4. Confirm Saturday/Sunday previews use the new athletic-development logic rather than equal-lane rotation.
5. In Barbell Strength Only, Sunday should prefer full Gym when productive Gym need is meaningful, then offer a true aerobic second session after the first workout is logged.
6. In Hybrid mode, fixed KB anchors stay fixed; compatible easy aerobic double sessions can be recommended instead of crowding endurance out of the week.
