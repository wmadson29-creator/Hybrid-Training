Hybrid Training v36.97

This is a full PWA package. Upload/extract the entire folder together so index.html can access sw.js, version.json, the manifest, icons, and research/QA files.

Primary changes from v36.96:
- direct Log navigation now resolves today’s current scheduled/recommended workout instead of restoring a stale selector-only session
- stale Log selector state can no longer contaminate the Dashboard recommendation
- real active workouts and substantive unsaved performance data remain protected
- removed literal build-shell \\n text that could render at the bottom-left of the page
- compact disclosure/source controls receive larger touch hit areas without adding visible vertical bulk
- Training Calendar appearance remains unchanged
- no training-model coefficient retune
