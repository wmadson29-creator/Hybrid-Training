# Deploy v36.111

Upload every file in this folder together to the repository root. Do not clear app data and do not re-import the export. After the installed app updates, open Today. The exact Sep 13 state should use the normal primary recommender and select Conditioning / LSS Run rather than the Active Recovery fallback.
