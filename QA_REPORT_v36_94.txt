Hybrid Training v36.94 — Readability / Regression QA Report
Date: 2026-09-12

PURPOSE
- Readability QA release on top of v36.93.
- Existing Training Calendar sizing and visual language intentionally preserved.
- No training-model coefficient, cadence, fatigue, progression, or fixed-anchor change is intended.

STATIC / PACKAGE
PASS — 416 static DOM IDs; 0 duplicates.
PASS — both production inline JavaScript blocks parse.
PASS — service worker parses.
PASS — manifest start_url = ./?v=36.94.
PASS — version.json = 36.94.
PASS — service-worker BUILD/cache aligned to 36.94.
PASS — visible Adaptive Engine label, export appBuild, storage diagnostic build stamp, and emergency-backup filename aligned to 36.94.

MODEL REGRESSION
PASS — 13 critical model/programming functions are byte-for-byte identical to v36.93:
recommendationFeedbackAdjustment; recommendationDoseNudge; rollingCadencePortfolio; rollingOpenDayRecommendation; adaptiveReadiness; utmRecentFatigue; easyAdaptivePick; hardAdaptivePick; conditioningFor; pullupForWeek; getTBPlan; adaptiveSEPrescription; smartDailyAddOnFor.

SCHEDULE SANITY
- Upcoming automatic mix remained appropriately multimodal in the inspected horizon.
- Running remains the first cardio priority.
- Projected conditioning sequence includes easy running, occasional LSS Swim, and periodic Speed/HIC work.
- No treadmill running was automatically selected in the inspected future sequence while outdoor-running preference is active.
- Calisthenics remains available but did not dominate automatic open-day recommendations.
- No model retuning was applied because the inspected behavior matched the intended preferences.

READABILITY QA
PASS — research summary metadata increased to ~11.2 px at the 390 px phone viewport.
PASS — research confidence labels increased to ~10.6 px.
PASS — research claim/source metadata increased to ~11.0 px.
PASS — research source-use/app-derived text increased to ~11.2 px.
PASS — research reviewed/provenance line increased to ~10.7 px.
PASS — storage diagnostic labels increased to ~10.4 px.
PASS — workout-tracker helper text and Trends statistic labels received a modest readability increase.
PASS — Training Calendar text sizing was not touched by the v36.94 override.

RESPONSIVE / RUNTIME
PASS — 15 major views × 4 widths (320 / 390 / 768 / 1440 px): 60 combinations.
PASS — 0 document-level or active-view horizontal-overflow cases.
PASS — 0 browser runtime page errors in the v36.94 sweep.
PASS — 0 console errors in the v36.94 sweep.

VISUAL REVIEW
- Generic Gym builder remains readable at 390 px without overflow.
- Settings remains stable at 390 px.
- Log retains the v36.93 Session setup → Workout → Finish hierarchy; increased helper readability did not reintroduce the prior boxed/cluttered layout.

ENVIRONMENT LIMIT
A true Chromium localhost/installed-PWA navigation is administrator-blocked in this runtime. Package files and service-worker/version alignment are statically validated; browser runtime QA uses the same production HTML through an opaque test origin with a storage shim.

FINAL STATUS
v36.94 passes static, model-regression, readability, responsive, and runtime QA. It is an intentionally low-risk readability release on top of the validated v36.93 model.
